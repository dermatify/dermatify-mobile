<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground">
    <ImageView
        android:id="@+id/iv_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:src="@drawable/ic_change_lang"
        app:layout_constraintTop_toTopOf="@id/tv_setting_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/tv_setting_title"/>

    <TextView
        android:id="@+id/tv_setting_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Change Language"
        android:layout_marginStart="8dp"
        android:textStyle="bold"
        android:textSize="16sp"
        android:textColor="@color/grey_variant_3"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/iv_icon" />

    <TextView
        android:id="@+id/tv_setting_subtitle"
        android:layout_marginTop="4dp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="Pick the language setting you’d like to use."
        android:alpha="0.7"
        android:textSize="12sp"
        android:textColor="@color/grey_variant_3"
        app:layout_constraintTop_toBottomOf="@id/tv_setting_title"
        app:layout_constraintStart_toStartOf="@id/tv_setting_title"
        app:layout_constraintEnd_toStartOf="@id/iv_next"/>

    <ImageView
        android:id="@+id/iv_next"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_next_1"
        android:alpha="0.5"
        app:tint="@color/grey_variant_3"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_setting_title"
        app:layout_constraintBottom_toBottomOf="@id/tv_setting_subtitle" />

</androidx.constraintlayout.widget.ConstraintLayout>